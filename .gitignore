# =========================
# Monorepo .gitignore (Node/Next + Rust + Terraform)
# =========================

# Node / Next.js (recursive for packages/apps)
node_modules/
**/node_modules/
.next/
**/.next/
dist/
**/dist/
build/
**/build/
out/
**/out/
coverage/
**/coverage/
playwright-report/
**/playwright-report/
test-results/
**/test-results/

# Logs
*.log
*.cpuprofile
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Environment files
.env
.env.*
!.env.example
.dev.vars
**/.dev.vars
.wrangler/
**/.wrangler/

# Rust build artifacts
debug/
target/
**/target/
**/*.rs.bk
# Cargo.lock is committed for applications to ensure reproducible builds

# Terraform state and caches
.terraform/
**/.terraform/
terraform.tfstate
terraform.tfstate.*
**/terraform.tfstate
**/terraform.tfstate.*
# Plan files: `terraform plan -out=tfplan` produces an extensionless file, which
# *.tfplan does not match. Both forms are ignored — a plan embeds state, so it
# can carry resource attribute values.
**/*.tfplan
tfplan
**/tfplan
**/crash.log
terraform.tfvars
**/terraform.tfvars

# Claude Code
.claude/

# Codex CLI
.codex/

# Python
__pycache__/
*.pyc

# OS / Editor
.DS_Store
.vscode/
**/.vscode/
.idea/
**/.idea/

# Misc
*.local
uploads/

# Test files
apps/web/test-seedrandom.js
