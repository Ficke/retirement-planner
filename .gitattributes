# Wrangler regenerates this from wrangler.jsonc. It is committed so a fresh
# clone typechecks and `wrangler types --check` can detect staleness, but it
# should not be reviewed by hand or counted in language statistics.
apps/web/worker-configuration.d.ts linguist-generated=true whitespace=-trailing-space
pnpm-lock.yaml linguist-generated=true
